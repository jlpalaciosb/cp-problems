#include <iostream>
#include <vector>
using namespace std;
typedef long long ll;
typedef vector<int> vi;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	int t; cin >> t;
	while (t--) {
		cout << sizeof((float)1.0) << endl;
		cout << sizeof(1.0) << endl;
	}

	return 0;
}
